import CreateMeetupButton from "./components/CreateMeetupButton";

const Header = () => {
    return (
        <header className="pt-8 pb-7 w-full">
            <CreateMeetupButton />
        </header>
    );
};

export default Header;
