import CreateMeetupButton from "./components/CreateMeetupButton";

const Header = () => {
    return (
        <header className="bg-red-100 pt-[42px] pb-[60px] w-full">
            <CreateMeetupButton />
        </header>
    );
};

export default Header;
