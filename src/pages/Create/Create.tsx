import { Swiper, SwiperSldie } from "swiper/react";
import { Pagination } from "swiper/modules";

const Create = () => {
    return (
        <div>
            <h1>Create</h1>
        </div>
    );
};

export default Create;
